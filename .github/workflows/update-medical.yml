name: Update medical data

on:
  schedule:
    - cron: "17 20 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 테스트
        run: npm test

      - name: 데이터 수집
        run: node scripts/fetch-medical.mjs
        env:
          PHARMACY_API_KEY: ${{ secrets.PHARMACY_API_KEY }}
          PHARMACY_API_ENDPOINT: ${{ secrets.PHARMACY_API_ENDPOINT }}
          EMERGENCY_API_KEY: ${{ secrets.EMERGENCY_API_KEY }}
          EMERGENCY_API_ENDPOINT: ${{ secrets.EMERGENCY_API_ENDPOINT }}

      - name: 변경사항 커밋 및 푸시
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data
          if git diff --cached --quiet; then
            echo "변경사항 없음, 커밋 생략"
            exit 0
          fi
          git commit -m "chore: 약국·응급의료기관 데이터 갱신 $(TZ=Asia/Seoul date +%Y-%m-%d)"
          for i in 1 2 3 4 5; do
            if git push; then
              exit 0
            fi
            echo "push 실패($i), rebase 후 재시도"
            git pull --rebase origin "$(git rev-parse --abbrev-ref HEAD)"
            sleep $((i * 3))
          done
          echo "push 재시도 모두 실패"
          exit 1

      - name: Pages 설정
        uses: actions/configure-pages@v5

      - name: Pages 아티팩트 업로드
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Pages에 배포
        id: deployment
        uses: actions/deploy-pages@v4
